import type { LCombo } from "./LCombo"

export class Type {
    id?: number
    internalName?: string
    name?: string
    weakness?: LCombo
    resistences?: LCombo
    inmunities?: LCombo
    icon?: string
}